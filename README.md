# Django-Twitter-Lite-Project
A lightweight Twitter-like web application built using Django where users can create, view, search, edit, and delete tweets. The application implements authentication features such as login, logout, and user registration.It also supports image uploads and a search functionality to filter tweets.
